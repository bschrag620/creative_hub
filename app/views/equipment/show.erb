<h2><%=@equip.name%></h2>
Currently in use:  <%=@equip.in_use%> 
<% if !@equip.in_use %>
	<br><a href="<%=@equip.id%>/use"><button>Use equipment</button></a>
<% end %><hr>
Required certification:
	<ul>
		<% if @equip.certificate %>
			<li><a href="/certificates/<%=@equip.certificate.id%>"><%=@equip.certificate.name%></a></li>
		<% else %>
			<% if current_user.is_admin %>
				No certificate specified. Choose <a href="/equipment/<%=@equip.id%>/edit">edit</a> to fix.
			<% else %>
				No certificate specified, notify admin to correct the issue.
			<% end %>
		<% end %>
	</ul>
<% if current_user.is_admin %>
	<a href="/equipment/<%=@equip.id%>/edit"><button>Edit equipment</button></a>
		<form action="/equipment/<%=@equip.id%>" method="post">
			<input type="hidden" name="_method" value="delete"><button>Delete equipment</button>
		</form> <hr>	
<% end %>
